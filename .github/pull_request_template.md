## What

<!-- One sentence: what does this PR do? -->

## Why

<!-- Why is this change needed? Link to an issue if one exists. -->

## Test plan

- [ ] Tests added or updated for the changed behaviour
- [ ] `pytest -m "not slow"` passes locally
- [ ] `ruff check .` passes locally

## Breaking changes

<!-- Does this change the ProfileReport schema, the hook API, or the CLI output format?
     If yes, describe what breaks and what callers need to update. -->
